<h1>Welcome To NEW Message Page<h1>


<div>

<%= form_for @message, url: messages_path, method: :post do |f| %>
  
     <div class="text">
    <%= f.text_area :text, placeholder: "text" %> 
    </div><!-- text -->

    <div class="number">
    <%= f.telephone_field :number, placeholder: "Enter phone numbers here" %> 
    </div><!-- number -->

  <!-- uploadcare -->
  
  
  <%= f.submit "Send Message", class: 'btn btn-primary' %>
 

<% end %>

<% @contacts.each do |contact| %>
	<%= "#{contact.first_name} #{contact.last_name}: #{contact.number}" %>
<% end %>
</div>